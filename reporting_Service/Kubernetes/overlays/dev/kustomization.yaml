bases:
  - ../../base

namespace: dev

namePrefix: dev-

# patchesStrategicMerge:
#   - replicas.yaml
#   - ports.yaml

configMapGenerator:
- envs:
  - config.properties
  name: reporting-config

labels:
- includeSelectors: true
  pairs:
    app: dev-reporting-service

images:
- name: mohamedsamir170/reporting-service
  newTag: dev