bases:
  - ../../base

namespace: prod

namePrefix: prod-

patchesStrategicMerge:
  - replicas.yaml
  - ports.yaml

configMapGenerator:
- envs:
  - config.properties
  name: user-config
labels:
- includeSelectors: true
  pairs:
    app: prod-user-service
images:
- name: mohamedsamir170/user-service
  newTag: latest